{
  "name": "@bridgebyte/sst-test-vue",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Browser smoke-test app for @bridgebyte/sst-core + @bridgebyte/sst-vue consumed via local Verdaccio.",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@bridgebyte/sst-core": "^0.2.0",
    "@bridgebyte/sst-vue": "^0.1.0",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.1.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vue-tsc": "^2.1.0"
  }
}
